#pragma once

#include <SFML/Graphics.hpp>

const sf::Vector2i OBJ_DATA_SIZE(3, 2);
const sf::Vector2f DEF_POS(112, 56);
const sf::Vector2f GAME_BOY_POS(150, 0);
const sf::Vector2f GAME_BOY_SCALE(1.5, 1);
const sf::Vector2f TEXT_FINISH_POS(500, 40);

const sf::FloatRect VIEW_RECT(0.25f, 0.05f, 0.5f, 0.5f);

const auto THOUSAND = 1000;
const auto TEXT_FINISH_SIZE = 75u;

const auto TEXT_FINISH_OUT_LINE_SIZE = 5.f;
const auto FOUR = 4.f;
const auto HIGHT_Y_UPDATE = -3.f;
const auto HIGHT_Y = -17.f;
const auto FOV = 300.f;
const auto HUNDRED = 100.f;
const auto TIME_RACE = 60.f;
const auto FIFTHY = 50.f;
const auto TENTH = 0.1f;
const auto EIGHT = 8.f;

const auto START_POS_TXT = "_start_position.txt";


